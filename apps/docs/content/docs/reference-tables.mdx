---
title: Tablas de referencia (Constantes)
description: Constantes tipadas del SDK y enlaces oficiales para datos de referencia externos en Factus API v2.
---

import { Callout } from "fumadocs-ui/components/callout";

Esta guía consolida las tablas oficiales de Factus y su equivalente en
constantes de `factus-js`.

Fuente oficial:
[Tablas de referencia](https://developers.factus.com.co/tablas-de-referencia/tablas)

## Modo de uso

Cada tabla de referencia se expone desde la raíz del paquete y sigue el siguiente patrón:

- **`*Code`** — mapa clave-valor para usar directamente en los campos de la API.
- **`*Info`** — Siempre incluye `description` y en algunos casos, como para documentos de identidad, también incluye `abbreviation`.

### Ejemplo

```ts
import { IdentityDocumentCode, IdentityDocumentCodeInfo } from "factus-js";

// Usar un código en un payload de la API
const docType = IdentityDocumentCode.CitizenshipCard; // "13"

// Descripción y abreviatura del código
const docInfo = IdentityDocumentCodeInfo[docType];
console.log(docInfo.description); // "Cédula de ciudadanía"
console.log(docInfo.abbreviation); // "CC"
```

<Callout type="info">
  Estas constantes están declaradas explícitamente y no realizan llamadas HTTP.
</Callout>

## Países, municipios, unidades de medida y monedas

Debido a su tamaño, estos datos no se incluyen como constantes dentro del SDK. Sin embargo, puedes consultarlos y descargarlos directamente desde la documentación oficial.

- [Países](https://developers.factus.com.co/tablas-de-referencia/countries/)
- [Municipios](https://developers.factus.com.co/tablas-de-referencia/municipios/)
- [Unidades de medida](https://developers.factus.com.co/tablas-de-referencia/unit-measures/)
- [Tipos de moneda](https://developers.factus.com.co/tablas-de-referencia/currency) — códigos ISO como `"USD"` o `"EUR"` para `CreateBillInput.currency.code`

## Tipo de documento de factura (`BillDocumentCode`)

```ts
import { BillDocumentCode, BillDocumentCodeInfo } from "factus-js";

const code = BillDocumentCode.ElectronicSalesInvoice;

console.log(code); // "01"
console.log(BillDocumentCodeInfo[code].description); // "Factura electronica de venta."
```

| Clave                          | Valor  | Descripción                                       |
| ------------------------------ | ------ | ------------------------------------------------- |
| `ElectronicSalesInvoice`       | `"01"` | Factura electrónica de venta.                     |
| `ElectronicTransmissionType03` | `"03"` | Instrumento electrónico de transmisión - tipo 03. |

## Tipo de operación (`OperationTypeCode`)

```ts
import { OperationTypeCode, OperationTypeCodeInfo } from "factus-js";

const code = OperationTypeCode.Standard;

console.log(code); // "10"
console.log(OperationTypeCodeInfo[code].description); // "Estándar."
```

| Clave                  | Valor            | Descripción                                             |
| ---------------------- | ---------------- | ------------------------------------------------------- |
| `Standard`             | `"10"`           | Estándar.                                               |
| `Mandates`             | `"11"`           | Mandatos.                                               |
| `Transport`            | `"12"`           | Transporte.                                             |
| `HealthCufe`           | `"SS-CUFE"`      | Sector salud - CUFE.                                    |
| `HealthReport`         | `"SS-Reporte"`   | Sector salud - Reporte.                                 |
| `HealthNoContribution` | `"SS-SinAporte"` | Sector salud - Sin aporte.                              |
| `HealthCollection`     | `"SS-Recaudo"`   | Sector salud - Recaudo de cuotas moderadoras o copagos. |

## Tipo de operación en nota crédito (`CreditNoteOperationCode`)

```ts
import {
  CreditNoteOperationCode,
  CreditNoteOperationCodeInfo,
} from "factus-js";

const code = CreditNoteOperationCode.WithReference;

console.log(code); // "20"
console.log(CreditNoteOperationCodeInfo[code].description); // "Nota Crédito que referencia una factura electronica."
```

| Clave              | Valor  | Descripción                                            |
| ------------------ | ------ | ------------------------------------------------------ |
| `WithReference`    | `"20"` | Nota crédito que referencia una factura electrónica.   |
| `WithoutReference` | `"22"` | Nota crédito sin referencia a una factura electrónica. |

## Concepto de corrección de nota crédito (`CreditNoteCorrectionCode`)

```ts
import {
  CreditNoteCorrectionCode,
  CreditNoteCorrectionCodeInfo,
} from "factus-js";

const code = CreditNoteCorrectionCode.PriceAdjustment;

console.log(code); // "4"
console.log(CreditNoteCorrectionCodeInfo[code].description); // "Ajuste de precio."
```

| Clave                           | Valor | Descripción                                                              |
| ------------------------------- | ----- | ------------------------------------------------------------------------ |
| `PartialReturn`                 | `"1"` | Devolución parcial de los bienes y/o no aceptación parcial del servicio. |
| `ElectronicInvoiceCancellation` | `"2"` | Anulación de factura electrónica.                                        |
| `PartialOrTotalDiscount`        | `"3"` | Rebaja o descuento parcial o total.                                      |
| `PriceAdjustment`               | `"4"` | Ajuste de precio.                                                        |
| `EarlyPaymentDiscount`          | `"5"` | Descuento comercial por pronto pago.                                     |
| `VolumeDiscount`                | `"6"` | Descuento comercial por volumen de ventas.                               |

## Estándar de producto (`ProductStandardCode`)

```ts
import { ProductStandardCode, ProductStandardCodeInfo } from "factus-js";

const code = ProductStandardCode.TaxpayerAdoption;

console.log(code); // "999"
console.log(ProductStandardCodeInfo[code].description); // "Estándar de adopción del contribuyente"
```

| Clave              | Valor   | Descripción                            |
| ------------------ | ------- | -------------------------------------- |
| `TaxpayerAdoption` | `"999"` | Estándar de adopción del contribuyente |
| `UNSPSC`           | `"001"` | UNSPSC                                 |
| `TariffHeading`    | `"020"` | Partida arancelaria                    |
| `GTIN`             | `"010"` | GTIN                                   |

## Concepto de reclamo (`ClaimConceptCode`)

```ts
import { ClaimConceptCode, ClaimConceptCodeInfo } from "factus-js";

const code = ClaimConceptCode.DocumentInconsistencies;

console.log(code); // "01"
console.log(ClaimConceptCodeInfo[code].description); // "Documento con inconsistencias"
```

| Clave                      | Valor  | Descripción                         |
| -------------------------- | ------ | ----------------------------------- |
| `DocumentInconsistencies`  | `"01"` | Documento con inconsistencias       |
| `GoodsNotDeliveredTotal`   | `"02"` | Mercancía no entregada totalmente   |
| `GoodsNotDeliveredPartial` | `"03"` | Mercancía no entregada parcialmente |
| `ServiceNotProvided`       | `"04"` | Servicio no prestado                |

## Código de evento (`EventCode`)

```ts
import { EventCode, EventCodeInfo } from "factus-js";

const code = EventCode.ExpressAcceptance;

console.log(code); // "033"
console.log(EventCodeInfo[code].description); // "Aceptación expresa"
```

| Clave                    | Valor   | Descripción                                     |
| ------------------------ | ------- | ----------------------------------------------- |
| `ReceiptAcknowledgement` | `"030"` | Acuse de recibo de Factura Electronica de Venta |
| `BillClaim`              | `"031"` | Reclamo de la Factura Electronica de Venta      |
| `GoodsOrServiceReceipt`  | `"032"` | Recibo del bien y/o prestación del servicio     |
| `ExpressAcceptance`      | `"033"` | Aceptación expresa                              |
| `TacitAcceptance`        | `"034"` | Aceptación tacita                               |

## Tipo de documento de identidad (`IdentityDocumentCode`)

```ts
import { IdentityDocumentCode, IdentityDocumentCodeInfo } from "factus-js";

const code = IdentityDocumentCode.CitizenshipCard;

console.log(code); // "13"
console.log(IdentityDocumentCodeInfo[code].abbreviation); // "CC"
console.log(IdentityDocumentCodeInfo[code].description); // "Cédula de ciudadanía"
```

| Clave                           | Valor  | Abreviatura | Descripción                            |
| ------------------------------- | ------ | ----------- | -------------------------------------- |
| `CivilRegistration`             | `"11"` | RC          | Registro civil                         |
| `IdentityCard`                  | `"12"` | TI          | Tarjeta de identidad                   |
| `CitizenshipCard`               | `"13"` | CC          | Cédula de ciudadanía                   |
| `ForeignerIdentityCard`         | `"21"` | TE          | Tarjeta de extranjería                 |
| `ForeignerId`                   | `"22"` | CE          | Cédula de extranjería                  |
| `NIT`                           | `"31"` | NIT         | NIT                                    |
| `Passport`                      | `"41"` | PA          | Pasaporte                              |
| `ForeignIdentificationDocument` | `"42"` | DE          | Documento de identificación extranjero |
| `PEP`                           | `"47"` | PEP         | PEP                                    |
| `PPT`                           | `"48"` | PPT         | Permiso de Protección Temporal         |
| `ForeignNIT`                    | `"50"` | NE          | NIT otro país                          |
| `NUIP`                          | `"91"` | NUIP        | NUIP                                   |

## Tipo de identidad para documento soporte (`SupportDocumentIdentityDocumentCode`)

```ts
import {
  SupportDocumentIdentityDocumentCode,
  SupportDocumentIdentityDocumentCodeInfo,
} from "factus-js";

const code = SupportDocumentIdentityDocumentCode.NIT;

console.log(code); // "31"
console.log(SupportDocumentIdentityDocumentCodeInfo[code].abbreviation); // "NIT"
console.log(SupportDocumentIdentityDocumentCodeInfo[code].description); // "NIT"
```

| Clave                           | Valor  | Abreviatura | Descripción                            |
| ------------------------------- | ------ | ----------- | -------------------------------------- |
| `ForeignerIdentityCard`         | `"21"` | TE          | Tarjeta de extranjería                 |
| `ForeignerId`                   | `"22"` | CE          | Cédula de extranjería                  |
| `NIT`                           | `"31"` | NIT         | NIT                                    |
| `Passport`                      | `"41"` | PA          | Pasaporte                              |
| `ForeignIdentificationDocument` | `"42"` | DE          | Documento de identificación extranjero |
| `PEP`                           | `"47"` | PEP         | PEP                                    |
| `ForeignNIT`                    | `"50"` | NE          | NIT otro país                          |

## Tributo del cliente (`CustomerTributeCode`)

```ts
import { CustomerTributeCode, CustomerTributeCodeInfo } from "factus-js";

const code = CustomerTributeCode.IVA;

console.log(code); // "01"
console.log(CustomerTributeCodeInfo[code].description); // "IVA"
```

| Clave           | Valor  | Descripción |
| --------------- | ------ | ----------- |
| `IVA`           | `"01"` | IVA         |
| `NotApplicable` | `"ZZ"` | No aplica   |

## Tipo de organización (`OrganizationTypeCode`)

```ts
import { OrganizationTypeCode, OrganizationTypeCodeInfo } from "factus-js";

const code = OrganizationTypeCode.NaturalPerson;

console.log(code); // "2"
console.log(OrganizationTypeCodeInfo[code].description); // "Persona Natural"
```

| Clave           | Valor | Descripción      |
| --------------- | ----- | ---------------- |
| `LegalEntity`   | `"1"` | Persona Jurídica |
| `NaturalPerson` | `"2"` | Persona Natural  |

## Método de pago (`PaymentMethodCode`)

```ts
import { PaymentMethodCode, PaymentMethodCodeInfo } from "factus-js";

const code = PaymentMethodCode.Transfer;

console.log(code); // "47"
console.log(PaymentMethodCodeInfo[code].description); // "Transferencia"
```

| Clave         | Valor   | Descripción               |
| ------------- | ------- | ------------------------- |
| `Cash`        | `"10"`  | Efectivo                  |
| `BankDeposit` | `"42"`  | Consignación              |
| `Check`       | `"20"`  | Cheque                    |
| `Transfer`    | `"47"`  | Transferencia             |
| `Bonds`       | `"71"`  | Bonos                     |
| `Vouchers`    | `"72"`  | Vales                     |
| `Undefined`   | `"1"`   | Medio de pago no definido |
| `DebitCard`   | `"49"`  | Tarjeta Débito            |
| `CreditCard`  | `"48"`  | Tarjeta Crédito           |
| `Other`       | `"ZZZ"` | Otro                      |

## Forma de pago (`PaymentFormCode`)

```ts
import { PaymentFormCode, PaymentFormCodeInfo } from "factus-js";

const code = PaymentFormCode.CreditPayment;

console.log(code); // "2"
console.log(PaymentFormCodeInfo[code].description); // "Pago a crédito"
```

| Clave           | Valor | Descripción     |
| --------------- | ----- | --------------- |
| `CashPayment`   | `"1"` | Pago de contado |
| `CreditPayment` | `"2"` | Pago a crédito  |

## Tipo de documento en rangos de numeración (`NumberingRangeDocumentTypeCode`)

```ts
import {
  NumberingRangeDocumentTypeCode,
  NumberingRangeDocumentTypeCodeInfo,
} from "factus-js";

const code = NumberingRangeDocumentTypeCode.SalesInvoice;

console.log(code); // "21"
console.log(NumberingRangeDocumentTypeCodeInfo[code].description); // "Factura de Venta"
```

| Clave                           | Valor  | Descripción                      |
| ------------------------------- | ------ | -------------------------------- |
| `SalesInvoice`                  | `"21"` | Factura de Venta                 |
| `CreditNote`                    | `"22"` | Nota Crédito                     |
| `DebitNote`                     | `"23"` | Nota Débito                      |
| `SupportDocument`               | `"24"` | Documento Soporte                |
| `SupportDocumentAdjustmentNote` | `"25"` | Nota de Ajuste Documento Soporte |
| `Payroll`                       | `"26"` | Nomina                           |
| `PayrollAdjustmentNote`         | `"27"` | Nota de Ajuste Nomina            |
| `PayrollDeletionNote`           | `"28"` | Nota de eliminación de nomina    |
| `PaperOrStubInvoice`            | `"30"` | Factura de talonario y de papel  |

## Concepto de corrección de nota de ajuste (`AdjustmentNoteReasonCode`)

```ts
import {
  AdjustmentNoteReasonCode,
  AdjustmentNoteReasonCodeInfo,
} from "factus-js";

const code = AdjustmentNoteReasonCode.PriceAdjustment;

console.log(code); // "4"
console.log(AdjustmentNoteReasonCodeInfo[code].description); // "Ajuste de precio"
```

| Clave                         | Valor | Descripción                                                                                                                           |
| ----------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `PartialReturn`               | `"1"` | Devolución parcial de los bienes y/o no aceptación parcial del servicio                                                               |
| `SupportDocumentCancellation` | `"2"` | Anulación del documento soporte en adquisiciones efectuadas a sujetos no obligados a expedir factura de venta o documento equivalente |
| `PartialOrTotalDiscount`      | `"3"` | Rebaja o descuento parcial o total                                                                                                    |
| `PriceAdjustment`             | `"4"` | Ajuste de precio                                                                                                                      |
| `Other`                       | `"5"` | Otros                                                                                                                                 |

## Tipo de cargo o descuento (`ChargeDiscountCode`)

```ts
import { ChargeDiscountCode, ChargeDiscountCodeInfo } from "factus-js";

const code = ChargeDiscountCode.ConditionalSurcharge;

console.log(code); // "03"
console.log(ChargeDiscountCodeInfo[code].description); // "Recargo condicionado"
```

| Clave                    | Valor  | Descripción               |
| ------------------------ | ------ | ------------------------- |
| `UnconditionalDiscount`  | `"00"` | Descuento no condicionado |
| `ConditionalDiscount`    | `"01"` | Descuento condicionado    |
| `UnconditionalSurcharge` | `"02"` | Recargo no condicionado   |
| `ConditionalSurcharge`   | `"03"` | Recargo condicionado      |

## Responsabilidad fiscal (`FiscalResponsibilityCode`)

```ts
import {
  FiscalResponsibilityCode,
  FiscalResponsibilityCodeInfo,
} from "factus-js";

const code = FiscalResponsibilityCode.NotResponsible;

console.log(code); // "R-99-PN"
console.log(FiscalResponsibilityCodeInfo[code].description); // "No responsable"
```

| Clave                  | Valor       | Descripción                   |
| ---------------------- | ----------- | ----------------------------- |
| `LargeTaxpayer`        | `"O-13"`    | Gran contribuyente            |
| `SelfWithholdingAgent` | `"0-15"`    | Autorretenedor                |
| `VATWithholdingAgent`  | `"0-23"`    | Agente de retención de IVA    |
| `SimpleTaxRegime`      | `"0-47"`    | Regimen simple de tributación |
| `NotResponsible`       | `"R-99-PN"` | No responsable                |
