---
title: Rangos asociados al software
description: Obtener los rangos de numeración asociados al software.
---

## Firma del método

```ts
getSoftwareRange(
  options?: RequestOptions,
): Promise<ApiResponse<SoftwareNumberingRange[]>>
```

## Endpoint Factus

`GET /v2/numbering-ranges/dian`

Fuente oficial: [Rangos asociados al software](https://developers.factus.com.co/rangos-de-numeracion/rangos-asociados-al-software)

## Parámetros

Este método no recibe parámetros.

## Ejemplo

```ts
const response = await factus.numberingRanges.getSoftwareRange();
const numberingRanges = response.data;

for (const numberingRange of numberingRanges) {
  console.log(numberingRange.prefix, numberingRange.from, numberingRange.to);
}
```

{/* using response example from v1. TODO: update to table once available */}

## Ejemplo de respuesta

```json
{
  "status": "OK",
  "message": "Solicitud exitosa",
  "data": [
    {
      "resolution_number": "18760000007",
      "prefix": "SEDS",
      "from": "984000000",
      "to": "985000000",
      "start_date": "2024-01-01",
      "end_date": "2024-12-31",
      "technical_key": ""
    }
  ]
}
```
