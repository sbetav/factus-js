---
title: Obtener rangos
description: Listar rangos de numeración con filtros y paginación.
---

## Firma del método

```ts
list(params?: ListParams<NumberingRangeFilters>): Promise<ApiResponse<PaginatedData<NumberingRange>>>
```

## Endpoint Factus

`GET /v1/numbering-ranges`

Fuente oficial: [Obtener rangos de numeración](https://developers.factus.com.co/rangos-de-numeracion/obtener-rangos/index.html)

## Parámetros

<TypeTable
  type={{
    params: {
      type: "ListParams<NumberingRangeFilters> (object)",
      required: false,
      description: (
        <div>
          Filtros y paginación para la consulta.
          <TypeTable
            type={{
              filter: {
                type: "NumberingRangeFilters",
                required: false,
                description: (
                  <TypeTable
                    type={{
                      id: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por ID interno del rango de numeración.",
                      },
                      document: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por tipo de documento asociado al rango.",
                      },
                      resolution_number: {
                        type: "string",
                        required: false,
                        description: "Filtra por número de resolución.",
                      },
                      technical_key: {
                        type: "string",
                        required: false,
                        description: "Filtra por clave técnica.",
                      },
                      is_active: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por estado activo del rango (`1` activo, `0` inactivo).",
                      },
                    }}
                  />
                ),
              },
              page: {
                type: "number",
                required: false,
                description: "Número de página (base 1).",
              },
              per_page: {
                type: "number",
                required: false,
                description: "Cantidad de resultados por página.",
              },
            }}
          />
        </div>
      ),
    },
  }}
/>

## Ejemplo

```ts
const response = await factus.numberingRanges.list({
  filter: { is_active: "1" },
  page: 1,
  per_page: 10,
});

const numberingRanges = response.data.data;
const pagination = response.data.pagination;
```
