---
title: Ver rango
description: Obtener el detalle de un rango de numeración por ID.
---

import { Callout } from "fumadocs-ui/components/callout";

## Firma del método

```ts
get(id: number, options?: RequestOptions): Promise<ApiResponse<NumberingRange>>
```

## Endpoint Factus

`GET /v2/numbering-ranges/{id}`

Fuente oficial: [Ver rango](https://developers.factus.com.co/rangos-de-numeracion/ver-rango)

## Parámetros

<TypeTable
  type={{
    id: {
      type: "number",
      required: true,
      description: "ID numérico del rango de numeración a consultar.",
    },
  }}
/>
## Ejemplo

```ts
const response = await factus.numberingRanges.get(4);
const numberingRange = response.data;

console.log(numberingRange.prefix);
console.log(numberingRange.current);
console.log(numberingRange.document);
```

<Callout type="warn">
  Es probable que este método no funcione en entorno sandbox. Úsalo en
  producción o con credenciales autorizadas.
</Callout>

## Respuesta

{/* using response table from v2 */}

<TypeTable
  type={{
    id: {
      type: "number",
      required: false,
      description: "ID del rango de numeración",
    },
    document: {
      type: "NumberingRangeDocumentTypeCode",
      required: false,
      description: "Número del documento",
    },
    document_name: {
      type: "string",
      required: false,
      description: "Nombre del documento",
    },
    prefix: {
      type: "string",
      required: false,
      description: "Prefijo del rango de numeración",
    },
    from: {
      type: "number",
      required: false,
      description: "Número de inicio del rango de numeración",
    },
    to: {
      type: "number",
      required: false,
      description: "Número del final del rango de numeración",
    },
    current: {
      type: "number",
      required: false,
      description: "Siguiente número dentro del rango de numeración",
    },
    resolution_number: {
      type: "string | null",
      required: false,
      description: "Número de resolución",
    },
    start_date: {
      type: "string",
      required: false,
      description: "Fecha en la que se expidió el rango de numeración",
    },
    end_date: {
      type: "string",
      required: false,
      description: "Fecha de vencimiento del rango de numeración",
    },
    technical_key: {
      type: "string | null",
      required: false,
      description: "Clave técnica",
    },
    is_expired: {
      type: "boolean",
      required: false,
      description:
        "El valor es 1 cuando el rango está vencido y 0 cuando está vigente",
    },
    is_active: {
      type: "boolean | 0 | 1",
      required: false,
      description:
        "El valor es 1 cuando el rango está activo y 0 cuando está inactivo",
    },
  }}
/>
