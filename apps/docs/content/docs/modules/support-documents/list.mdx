---
title: Filtrar documentos soporte
description: Listar documentos soporte con filtros y paginación.
---

## Firma del método

```ts
list(
  params?: ListParams<SupportDocumentFilters>,
  options?: RequestOptions,
): Promise<GetSupportDocumentsResponse>
```

## Endpoint Factus

`GET /v2/support-documents`

Fuente oficial: [Listar documentos soporte](https://developers.factus.com.co/documentos-soporte/ver-y-filtrar)

## Parámetros

<TypeTable
  type={{
    params: {
      type: "ListParams<SupportDocumentFilters> (object)",
      required: false,
      description: (
        <div>
          Filtros y paginación para la consulta.
          <TypeTable
            type={{
              filter: {
                type: "SupportDocumentFilters",
                required: false,
                description: (
                  <TypeTable
                    type={{
                      identification: {
                        type: "string",
                        required: false,
                        description: "Filtra por identificación del proveedor.",
                      },
                      names: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por nombres o razón social del proveedor.",
                      },
                      number: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por número del documento (admite coincidencias parciales).",
                      },
                      prefix: {
                        type: "string",
                        required: false,
                        description: "Filtra por prefijo del documento.",
                      },
                      reference_code: {
                        type: "string",
                        required: false,
                        description: "Filtra por código de referencia interno.",
                      },
                      status: {
                        type: "string | number",
                        required: false,
                        description:
                          "Estado de validación (`1` validado, `0` pendiente).",
                      },
                    }}
                  />
                ),
              },
              page: {
                type: "number",
                required: false,
                description: "Número de página (base 1).",
              },
              per_page: {
                type: "number",
                required: false,
                description: "Cantidad de resultados por página.",
              },
            }}
          />
        </div>
      ),
    },
  }}
/>

## Ejemplo

```ts
const response = await factus.supportDocuments.list({
  filter: { status: "1" },
  page: 1,
  per_page: 10,
});

const supportDocuments = response.data.data;
const pagination = response.data.pagination;
```

`response.data.data` contiene la página actual y `response.data.pagination` la metadata de paginación.

## Ejemplo de respuesta

{/* using response example from v1. TODO: update to table once available */}

```json
{
  "status": "OK",
  "message": "Solicitud exitosa",
  "data": {
    "data": [
      {
        "id": 6,
        "number": "SEDS984000021",
        "api_client_name": "[email protected]",
        "reference_code": "REF0017",
        "identification": "123456789",
        "graphic_representation_name": "Alan Turing",
        "trade_name": null,
        "names": "Alan Turing",
        "email": "[email protected]",
        "total": "90000.00",
        "status": 1,
        "errors": [
          "Regla: DSAB19b, Notificación: NIT del Prestador de Servicios no está autorizado por la DIAN"
        ],
        "created_at": "11-02-2025 10:16:18 PM",
        "adjustment_notes": []
      }
    ]
  }
}
```
