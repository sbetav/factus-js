---
title: Ver y filtrar facturas
description: Listar facturas con filtros y paginación.
---

## Firma del método

```ts
list(params?: ListParams<InvoiceFilters>): Promise<GetInvoicesResponse>
```

## Endpoint Factus

`GET /v1/bills`

Fuente oficial: [Ver y filtrar facturas](https://developers.factus.com.co/facturas/ver-y-filtrar/index.html)

## Parámetros

<TypeTable
  type={{
    params: {
      type: "ListParams<InvoiceFilters> (object)",
      required: false,
      description: (
        <div>
          Filtros y paginación para la consulta.
          <TypeTable
            type={{
              filter: {
                type: "InvoiceFilters",
                required: false,
                description: (
                  <TypeTable
                    type={{
                      identification: {
                        type: "string",
                        required: false,
                        description: "Filtra por identificación del cliente.",
                      },
                      names: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por nombres o razón social del cliente.",
                      },
                      number: {
                        type: "string",
                        required: false,
                        description:
                          "Filtra por número del documento (admite coincidencias parciales).",
                      },
                      prefix: {
                        type: "string",
                        required: false,
                        description: "Filtra por prefijo del documento.",
                      },
                      reference_code: {
                        type: "string",
                        required: false,
                        description: "Filtra por código de referencia interno.",
                      },
                      status: {
                        type: "string | number",
                        required: false,
                        description:
                          "Estado de validación (`1` validada, `0` pendiente).",
                      },
                    }}
                  />
                ),
              },
              page: {
                type: "number",
                required: false,
                description: "Número de página (base 1).",
              },
              per_page: {
                type: "number",
                required: false,
                description: "Cantidad de resultados por página.",
              },
            }}
          />
        </div>
      ),
    },
  }}
/>

## Ejemplo

```ts
const response = await factus.bills.list({
  filter: {
    status: "1",
    number: "SETP99",
  },
  page: 1,
  per_page: 15,
});

const facturas = response.data.data;
const paginacion = response.data.pagination;
```
