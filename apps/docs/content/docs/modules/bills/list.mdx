---
title: Ver y filtrar facturas
description: Listar facturas con filtros y paginación.
---

## Firma del método

```ts
list(filters?: InvoiceFilters & { page?: number; per_page?: number }): Promise<GetInvoicesResponse>
```

## Endpoint Factus

`GET /v1/bills`

Fuente oficial: [Ver y filtrar facturas](https://developers.factus.com.co/facturas/ver-y-filtrar/index.html)

## Parámetros

<TypeTable
  type={{
    filters: {
      type: "InvoiceFilters & { page?: number; per_page?: number } (object)",
      required: false,
      description: (
        <div>
          Filtros y paginación para la consulta.
          <TypeTable
            type={{
              "filter[identification]": {
                type: "string",
                required: false,
                description: "Filtra por identificación del cliente.",
              },
              "filter[names]": {
                type: "string",
                required: false,
                description: "Filtra por nombres o razón social del cliente.",
              },
              "filter[number]": {
                type: "string",
                required: false,
                description:
                  "Filtra por número del documento (admite coincidencias parciales).",
              },
              "filter[prefix]": {
                type: "string",
                required: false,
                description: "Filtra por prefijo del documento.",
              },
              "filter[reference_code]": {
                type: "string",
                required: false,
                description: "Filtra por código de referencia interno.",
              },
              "filter[status]": {
                type: "string | number",
                required: false,
                description:
                  "Estado de validación (`1` validada, `0` pendiente).",
              },
              page: {
                type: "number",
                required: false,
                description: "Número de página (base 1).",
              },
              per_page: {
                type: "number",
                required: false,
                description: "Cantidad de resultados por página.",
              },
            }}
          />
        </div>
      ),
    },
  }}
/>

## Ejemplo

```ts
const response = await factus.bills.list({
  "filter[status]": "1",
  "filter[number]": "SETP99",
  page: 1,
  per_page: 15,
});

const facturas = response.data.data;
const paginacion = response.data.pagination;
```
