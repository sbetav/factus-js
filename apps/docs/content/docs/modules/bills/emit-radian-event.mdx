---
title: Emitir eventos RADIAN
description: Emitir eventos RADIAN para una factura por número y tipo de evento.
---

import { Callout } from "fumadocs-ui/components/callout";

## Firma del método

```ts
emitRadianEvent(
  number: string,
  eventType: EventCode,
  input: RadianEventUpdateInput,
): Promise<RadianEventUpdateResponse>
```

## Endpoint Factus

`POST /v1/bills/radian/events/update/{number}/{event_type}`

Fuente oficial: [Aceptación tacita](https://developers.factus.com.co/facturas/aceptacion-tacita/index.html)

## Parámetros

<TypeTable
  type={{
    number: {
      type: "string",
      required: true,
      description:
        "Número del documento de la factura sobre la que se emitirá el evento.",
    },
    eventType: {
      type: "EventCode",
      required: true,
      description: (
        <>
          {"Código del evento RADIAN — Ver "}
          <a href="/docs/modules/catalog/reference-tables#código-de-evento-eventcode">
            {"EventCode"}
          </a>
          {"."}
        </>
      ),
    },
    input: {
      type: "RadianEventUpdateInput (object)",
      required: true,
      description: (
        <div>
          Datos para actualizar el evento RADIAN.
          <TypeTable
            type={{
            identification_document_code: {
              type: "IdentityDocumentTypeId",
              required: true,
              description: (
                <>
                  {"Tipo de documento de identificación de quien reporta el evento — Ver "}
                  <a href="/docs/modules/catalog/reference-tables#tipo-de-documento-de-identificaci%C3%B3n-identitydocumenttypeid">
                    {"IdentityDocumentTypeId"}
                  </a>
                  {"."}
                </>
              ),
            },
          identification: {
            type: "string",
            required: true,
            description:
              "Número de identificación de quien reporta el evento.",
          },
          dv: {
            type: "string",
            required: false,
            description:
              "Dígito de verificación (requerido si el tipo de documento es NIT).",
          },
          first_name: {
            type: "string",
            required: true,
            description: "Nombres de quien reporta el evento.",
          },
          last_name: {
            type: "string",
            required: true,
            description: "Apellidos de quien reporta el evento.",
          },
          job_title: {
            type: "string",
            required: false,
              description: "Cargo de quien reporta el evento.",
            },
            organization_department: {
              type: "string",
              required: false,
              description: "Área/departamento de quien reporta el evento.",
            },
            }}
          />
        </div>
      ),
    },

}}
/>

## Ejemplo

```ts
import { EventCode, IdentityDocumentTypeId } from "factus-js";

await factus.bills.emitRadianEvent(
  "SETP990000049",
  EventCode.TacitAcceptance.value,
  {
    identification_document_code: IdentityDocumentTypeId.CitizenshipId.value,
    identification: "12345667",
    first_name: "Pepito",
    last_name: "Perez",
    job_title: "Desarrollador de software",
    organization_department: "Sistemas",
  },
);
```

<Callout type="info">
  Este método cubre el flujo documentado por Factus para emisión de eventos
  RADIAN (incluyendo aceptación tacita, según el `eventType`).
</Callout>

<Callout type="warn">
  Es probable que este método no funcione en entorno sandbox. Úsalo en
  producción o con credenciales autorizadas.
</Callout>
