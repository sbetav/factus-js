---
title: Emitir evento
description: Emitir eventos RADIAN (030-034) para una factura recibida.
---

import { Callout } from "fumadocs-ui/components/callout";

## Firma del método

```ts
emitEvent(params: EmitEventParams, input: EmitEventInput): Promise<ApiResponse<ReceptionBillEvent[]>>
```

## Endpoint Factus

`PATCH /v1/receptions/bills/{bill_id}/radian/events/{event_type}`

Fuente oficial: [Emitir evento](https://developers.factus.com.co/recepcion-de-documentos/emitir-evento/index.html)

## Parámetros

<TypeTable
  type={{
    params: {
      type: "EmitEventParams",
      required: true,
      description: (
        <div>
          Identificación del documento y tipo de evento a emitir.
            <TypeTable
              type={{
                bill_id: {
                  type: "string | number",
                  required: true,
                  description:
                    "ID o número del documento recibido sobre el cual se emitirá el evento.",
                },
                event_type: {
                  type: "EventCode",
                  required: true,
                  description: (
                    <>
                      {"Código del evento RADIAN — Ver "}
                      <a href="/docs/modules/catalog/reference-tables#código-de-evento-eventcode">
                        {"EventCode"}
                      </a>
                      {"."}
                    </>
                  ),
                },
              }}
            />
        </div>
      ),
    },

    input: {
      type: "EmitEventInput (object)",
      required: true,
      description: (
        <div>
          Datos del evento a emitir.
          <TypeTable
            type={{
              claim_concept_code: {
      type: "ClaimConceptCode",
      required: false,
      description: (
        <>
          {"Concepto de reclamo; requerido cuando "}
          <code>{"event_type"}</code>
          {" es "}
          <code>{"031"}</code>
          {". — Ver "}
          <a href="/docs/modules/catalog/reference-tables#concepto-de-reclamo-claimconceptcode">
            {"ClaimConceptCode"}
          </a>
          {"."}
        </>
      ),
    },
    identification_document_code: {
      type: "number",
      required: true,
      description:
        "Tipo de documento de identificación de la persona que emite el evento.",
    },
    identification: {
      type: "string",
      required: true,
      description:
        "Número de identificación de la persona que emite el evento.",
    },
    dv: {
      type: "string",
      required: false,
      description: (
        <>
          {"Dígito de verificación; requerido cuando "}
          <code>{"identification_document_code"}</code>
          {" es NIT ("}
          <code>{"6"}</code>
          {")."}
        </>
      ),
    },
    first_name: {
      type: "string",
      required: true,
      description: "Nombres de la persona que emite el evento.",
    },
    last_name: {
      type: "string",
      required: true,
      description: "Apellidos de la persona que emite el evento.",
    },
    job_title: {
      type: "string",
      required: false,
      description: "Cargo de la persona que emite el evento.",
    },
    organization_department: {
      type: "string",
      required: false,
      description:
        "Área/departamento de la persona que emite el evento.",
    },
            }}
          />
        </div>
      ),
    },

}}
/>

## Ejemplo

```ts
import { EventCode, ClaimConceptCode } from "factus-js";

await factus.reception.emitEvent(
  {
    bill_id: "SETP990000203",
    event_type: EventCode.ReceiptAcknowledgement.value,
  },
  {
    identification_document_code: "13",
    identification: "12345667",
    first_name: "Pepito",
    last_name: "Perez",
    job_title: "Desarrollador de software",
    organization_department: "Sistemas",
  },
);

await factus.reception.emitEvent(
  {
    bill_id: "SETP990000203",
    event_type: EventCode.InvoiceClaim.value,
  },
  {
    claim_concept_code: ClaimConceptCode.DocumentInconsistencies.value,
    identification_document_code: "13",
    identification: "12345667",
    first_name: "Pepito",
    last_name: "Perez",
    job_title: "Desarrollador de software",
    organization_department: "Sistemas",
  },
);
```

<Callout type="info">
  Flujo usual: `030 -> 032 -> 033`. El evento `031` (reclamo) altera el flujo,
  y `034` (aceptación tácita) se genera automáticamente según reglas DIAN.
</Callout>

<Callout type="warn">
  Es probable que este método no funcione en entorno sandbox. Úsalo en
  producción o con credenciales autorizadas.
</Callout>
