---
"factus-js": minor
---

Add `abbreviation` to each entry in `IdentityDocumentTypeId` and `SupportDocumentIdentityTypeId` for UI labels and display. Document the mapping in the reference tables catalog page.
